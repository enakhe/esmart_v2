<Window x:Class="ESMART.Presentation.Forms.FrontDesk.Guest.GuestDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:ESMART.Presentation.Forms.FrontDesk.Guest"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#f0f4f9"
        ResizeMode="CanResize"
        Title="Guest Details" Height="700" Width="1300" Icon="/Forms/FrontDesk/Guest/logo.png" Activated="Window_Activated">

    <Window.Resources>
        <local:StatusToColorConverter x:Key="StatusToColorConverter" />
        <local:TransactionStatusToColor x:Key="TransactionStatusToColor" />
        <local:TransactionTypeColorConversion x:Key="TransactionTypeColorConversion" />
        <Style x:Key="ActionBarButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="#4ead16"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4ead16"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="3"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4ead16"/>
                </Trigger>

                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#4ead16"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1a237e"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>

        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="CanUserResizeColumns" Value="False" />
            <Setter Property="RowHeight" Value="50"/>
            <Setter Property="ColumnWidth" Value="*" />
            <Setter Property="ColumnHeaderStyle" Value="{StaticResource DataGridColumnHeaderStyle}"/>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="20,0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#1a237e" Grid.Column="0" Width="350" CornerRadius="10" Margin="10" Padding="10">
                <Grid Grid.Row="1">

                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <Grid>
                            <Border CornerRadius="5" Padding="10,3" HorizontalAlignment="Left" Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}" Height="23" Width="65">
                                <TextBlock Text="{Binding Status}" Foreground="White" HorizontalAlignment="Left"/>
                            </Border>
                            <Button Content="" x:Name="EditButton" HorizontalAlignment="Right" Height="23" Width="65" Margin="5" Background="#22C55E" Foreground="White" Cursor="Hand" Click="EditButton_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="10, 2">
                                                        <Grid>
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            <materialDesign:PackIcon Kind="Pencil" Width="15" Height="15" Foreground="White" />
                                                            <TextBlock Text="Edit" Foreground="White" HorizontalAlignment="Right"/>
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                        <StackPanel>
                            <Ellipse Width="100" Height="100">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding GuestImage}" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="{Binding FullName}" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="SemiBold" Foreground="White" Margin="10,5,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Row="1">
                        <Grid Margin="0, 0, 0, 20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Email:" HorizontalAlignment="Left" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Email}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0, 0, 0, 20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Phone Number:" HorizontalAlignment="Left" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding PhoneNumber}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0, 0, 0, 20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Gender:" HorizontalAlignment="Left" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Gender}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0, 0, 0, 20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Street:" HorizontalAlignment="Left" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Street}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0, 0, 0, 20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="City:" HorizontalAlignment="Left" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding City}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0, 0, 0, 20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Country:" HorizontalAlignment="Left" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Country}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0, 0, 0, 20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Current Balance:" HorizontalAlignment="Left" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding CurrentBalance, StringFormat=N2}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0, 0, 0, 20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Created By:" HorizontalAlignment="Left" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding CreatedBy}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0, 0, 0, 20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Date Created:" HorizontalAlignment="Left" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding DateCreated}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0, 0, 0, 20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Date Modified:" HorizontalAlignment="Left" FontWeight="SemiBold" Foreground="White" Margin="10,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding DateModified}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <Separator Margin="0,10" Grid.Row="2" Foreground="#e0e0e0" VerticalAlignment="Bottom" Background="#e0e0e0" />

                    <StackPanel Orientation="Vertical" VerticalAlignment="Bottom" Grid.Row="3">

                        <Button Content="Fund Account" Height="34" Margin="5" Padding="0,5,0,5" Background="#22C55E" Foreground="White" Cursor="Hand" Click="Button_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="5">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Content="Print Folio" Height="34" x:Name="PrintButton" Margin="5" Foreground="#1E3A8A" Padding="0,5,0,5" Background="#F1F5F9" Cursor="Hand" Click="PrintButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="5">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Content="Print Guest" Height="34" Margin="5" x:Name="PrintGuest" Foreground="#111827" Padding="0,5,0,5" Background="#F1F5F9" Cursor="Hand" Click="PrintGuest_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="5">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Content="Delete" Height="34" Margin="5" Padding="0,5,0,5" Background="Red" Foreground="White" Cursor="Hand" Click="DeleteGuest_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="5">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>

                </Grid>
            </Border>

            <Border Background="White" Height="Auto" CornerRadius="10" Padding="5" Grid.Column="1" Margin="0,0,0,10">
                <TabControl Margin="10" Style="{StaticResource MaterialDesignTabControl}" SelectionChanged="TabControl_SelectionChanged">

                    <TabItem Header="Guest Folio" Name="tbTransactionHistory">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock FontSize="20" Margin="0,20,0,0" HorizontalAlignment="Center" Text="Booking Invoice List (Invoices Unsettled)" Grid.Row="0" />
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden">
                                    <Border Background="White" CornerRadius="10" Margin="0,10,0,0" Grid.Row="1">
                                        <DataGrid x:Name="TransactionItemDataGrid" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding}" RowDetailsVisibilityMode="VisibleWhenSelected" Margin="20" Width="1440">
                                            <DataGrid.Columns>
                                                <DataGridTemplateColumn Header="Guest Name" Width="2*">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                                                                <TextBlock Text="{Binding GuestName}" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="Black" Margin="10,0,0,0"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>

                                                <DataGridTemplateColumn Header="Invoice" Width="*">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                                                                <TextBlock Text="{Binding Invoice}" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="Black" Margin="10,0,0,0"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>

                                                <DataGridTemplateColumn Header="Amount" Width="*">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                                                                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Foreground="Black" Margin="10,0,0,0">
                                        <Run Text="₦" />
                                        <Run Text="{Binding Amount, StringFormat=N2}" />
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>

                                                <DataGridTemplateColumn Header="Discount" Width="*">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                                                                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Foreground="Black" Margin="10,0,0,0">
                                        <Run Text="₦" />
                                        <Run Text="{Binding Discount, StringFormat=N2}" />
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>

                                                <DataGridTemplateColumn Header="Tax" Width="*">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                                                                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Foreground="Black" Margin="10,0,0,0">
                                        <Run Text="₦" />
                                        <Run Text="{Binding Tax, StringFormat=N2}" />
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>

                                                <DataGridTemplateColumn Header="Other Charges" Width="*">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                                                                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Foreground="Black" Margin="10,0,0,0">
                                        <Run Text="₦" />
                                        <Run Text="{Binding OtherCharges, StringFormat=N2}" />
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>

                                                <DataGridTemplateColumn Header="Paid" Width="*">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <Border CornerRadius="5" Height="23" HorizontalAlignment="Stretch" Padding="10,3">
                                                                <TextBlock Foreground="Black" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Run Text="₦" />
                                        <Run Text="{Binding Paid, StringFormat=N2}" />
                                                                </TextBlock>
                                                            </Border>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>

                                                <DataGridTemplateColumn Header="Refunds" Width="*">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                                                                <TextBlock Text="{Binding Refunds, StringFormat=₦ {0:N2}}" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="Black" Margin="10,0,0,0" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>

                                                <DataGridTemplateColumn Header="Balance" Width="2*">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                                                                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Foreground="Black" Margin="10,0,0,0">
                                        <Run Text="₦" />
                                        <Run Text="{Binding Balance, StringFormat=N2}" />
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>
                                            </DataGrid.Columns>

                                            <DataGrid.RowDetailsTemplate>
                                                <DataTemplate>
                                                    <StackPanel Margin="10">
                                                        <GroupBox Header="Room Charges">
                                                            <DataGrid ItemsSource="{Binding BookingGroups}" AutoGenerateColumns="False" >
                                                                <DataGrid.Columns>
                                                                    <DataGridTemplateColumn Header="Details">
                                                                        <DataGridTemplateColumn.CellTemplate>
                                                                            <DataTemplate>
                                                                                <Expander Header="{Binding GuestName}" HorizontalAlignment="Stretch" FocusVisualStyle="{x:Null}">
                                                                                    <StackPanel>
                                                                                        <TextBlock FontSize="14" Margin="0,0,20,10" FontWeight="Bold" Text="{Binding Summary}" HorizontalAlignment="Right"/>
                                                                                        <DataGrid IsReadOnly="True" ItemsSource="{Binding RecentTransactions}" AutoGenerateColumns="False">
                                                                                            <DataGrid.Columns>
                                                                                                <DataGridTextColumn Width="100" Header="Date" Binding="{Binding Date}" />
                                                                                                <DataGridTextColumn Width="500" Header="Description" Binding="{Binding Description}" />
                                                                                                <DataGridTextColumn Width="150" Header="Invoice" Binding="{Binding Invoice}" />
                                                                                                <DataGridTextColumn Width="150" Header="Discount" Binding="{Binding Discount, StringFormat=₦ {0:N2}}" />
                                                                                                <DataGridTextColumn Width="150" Header="Bill Posts" Binding="{Binding BillPosts, StringFormat=₦ {0:N2}}" />
                                                                                                <DataGridTextColumn Width="150" Header="Amount" Binding="{Binding Amount, StringFormat=₦ {0:N2}}" />
                                                                                                <DataGridTextColumn Width="150" Header="Payment" Binding="{Binding Payment, StringFormat=₦ {0:N2}}" />
                                                                                            </DataGrid.Columns>
                                                                                        </DataGrid>
                                                                                    </StackPanel>
                                                                                </Expander>
                                                                            </DataTemplate>
                                                                        </DataGridTemplateColumn.CellTemplate>
                                                                    </DataGridTemplateColumn>
                                                                </DataGrid.Columns>
                                                            </DataGrid>
                                                        </GroupBox>

                                                        <GroupBox Header="Service Consumptions" Margin="0,10,0,0">
                                                            <DataGrid IsReadOnly="True" ItemsSource="{Binding ServiceConsumptions}" AutoGenerateColumns="False">
                                                                <DataGrid.Columns>
                                                                    <DataGridTextColumn Width="100" Header="Date" Binding="{Binding Date}" />
                                                                    <DataGridTextColumn Width="500" Header="Description" Binding="{Binding Description}" />
                                                                    <DataGridTextColumn Width="150" Header="Invoice" Binding="{Binding Invoice}" />
                                                                    <DataGridTextColumn Width="150" Header="Discount" Binding="{Binding Discount}" />
                                                                    <DataGridTextColumn Width="150" Header="Bill Posts" Binding="{Binding BillPosts, StringFormat=₦ {0:N2}}" />
                                                                    <DataGridTextColumn Width="150" Header="Amount" Binding="{Binding Amount, StringFormat=₦ {0:N2}}" />
                                                                    <DataGridTextColumn Width="150" Header="Payment" Binding="{Binding Payment, StringFormat=₦ {0:N2}}" />
                                                                </DataGrid.Columns>
                                                            </DataGrid>
                                                        </GroupBox>

                                                        <GroupBox Header="Payments" Margin="0,10,0,0">
                                                            <DataGrid IsReadOnly="True" ItemsSource="{Binding Payments}" AutoGenerateColumns="False">
                                                                <DataGrid.Columns>
                                                                    <DataGridTextColumn Width="100" Header="Date" Binding="{Binding Date}" />
                                                                    <DataGridTextColumn Width="500" Header="Description" Binding="{Binding Description}" />
                                                                    <DataGridTextColumn Width="150" Header="Invoice" Binding="{Binding Invoice}" />
                                                                    <DataGridTextColumn Width="150" Header="Discount" Binding="{Binding Discount,StringFormat=₦ {0:N2}}" />
                                                                    <DataGridTextColumn Width="150" Header="Bill Posts" Binding="{Binding BillPosts, StringFormat=₦ {0:N2}}" />
                                                                    <DataGridTextColumn Width="150" Header="Amount" Binding="{Binding Amount, StringFormat=₦ {0:N2}}" />
                                                                    <DataGridTextColumn Width="150" Header="Payment" Binding="{Binding Payment, StringFormat=₦ {0:N2}}" />
                                                                </DataGrid.Columns>
                                                            </DataGrid>
                                                        </GroupBox>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGrid.RowDetailsTemplate>
                                        </DataGrid>
                                    </Border>
                                </ScrollViewer>
                                
                                <StackPanel Grid.Row="2" Width="500" x:Name="SummaryPanel" Margin="20" HorizontalAlignment="Right">
                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" x:Name="txtSummaryName" FontWeight="SemiBold" FontSize="15" Text=""  />
                                        <TextBlock Grid.Column="1" x:Name="txtBookingAmount" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="15" Text="₦ 0.00" />
                                    </Grid>
                                    
                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="15" Text="Discount" />
                                        <TextBlock Grid.Column="1" x:Name="txtDiscount" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="15" Text="₦ 0.00" />
                                    </Grid>

                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="15" Text="Service Charge" />
                                        <TextBlock Grid.Column="1" x:Name="txtServiceCharge" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="15" Text="₦ 0.00" />
                                    </Grid>

                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="15" Text="VAT" />
                                        <TextBlock Grid.Column="1" x:Name="txtVAT" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="15" Text="₦ 0.00" />
                                    </Grid>

                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="15" Text="Total Amount" />

                                        <StackPanel Grid.Column="1" >
                                            <Separator />
                                            <TextBlock HorizontalAlignment="Right" x:Name="txtTotalAmount" FontWeight="SemiBold" FontSize="15" Text="₦ 0.00" />
                                        </StackPanel>
                                    </Grid>

                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="15" Text="Amount Paid" />

                                        <StackPanel Grid.Column="1" >
                                            <TextBlock HorizontalAlignment="Right" x:Name="txtAmountPaid" FontWeight="SemiBold" FontSize="15" Text="₦ 0.00" />
                                            <Separator />
                                        </StackPanel>
                                    </Grid>

                                    <Grid Margin="5" x:Name="ReceiveGrid" Visibility="Collapsed">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="15" Text="Amount to be received" />
                                        <TextBlock Grid.Column="1" x:Name="txtReceive" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="15" Text="₦ 0.00" />
                                    </Grid>

                                    <Grid Margin="5" x:Name="RefundGrid" Visibility="Collapsed">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="15" Text="Amount to be refunded" />
                                        <TextBlock Grid.Column="1" x:Name="txtRefund" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="15" Text="₦ 0.00" />
                                    </Grid>

                                    <TextBlock x:Name="txtAccountBalanced" Visibility="Collapsed" FontSize="15" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="Green" Text="Account Balanced" />
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                        
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>


        <Grid x:Name="LoaderOverlay" Background="Black" Opacity="0.5" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="20" />
                <TextBlock Text="Loading, please wait..." Foreground="White" FontWeight="Bold" Margin="10,5,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
